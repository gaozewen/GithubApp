/**
 * 时间间隔 (TrendingPage 使用)
 *
 * @export
 * @class Timespan
 */
export default class Timespan {
  constructor(showText, searchText) {
    this.showText = showText
    this.searchText = searchText
  }
}
